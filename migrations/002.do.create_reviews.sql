CREATE TABLE reviews (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    content TEXT NOT NULL,
    plantId INTEGER
        REFERENCES plants(id) ON DELETE CASCADE NOT NULL,
    rating INTEGER NOT NULL
);